{
  "name": "prototype",
  "private": true,
  "scripts": {
    "install": "npm install --workspaces",
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm start --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "dependencies": {
    "serve-favicon": "^2.5.0"
  }
}
